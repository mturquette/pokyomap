# Distributed/dchroot bitbake setup
# 
# Note: this file should be shell script syntax, not bitbake syntax.. but you
# should probably limit this file to setting some variables so it could also
# be used directly from bitbake...

# name of the dchroot chroot filesystem:
DCHROOT_NAME="buildroot"

# list of servers available to build on:
BUILD_SERVERS="polarbear \
               sealion \
               penguin"

